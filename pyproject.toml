[project]
name = "leadradar"
version = "0.1.0"
description = "Find and score potential customers for RouterLink from public developer signals."
requires-python = ">=3.10"
dependencies = []

[project.scripts]
leadradar = "leadradar.cli:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
